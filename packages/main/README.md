# Package main

A runnable consumer application of the `jrjs` project.

Emulates a `jrjs-template` derived repo implementation, importing the `jrjs` dependency.

## Run

From the repo's top level:

`npm run start`

It will clone shared source code and run `main/sys/index.js`, launching a server, as configured in `main/sys/config.js` by default.
